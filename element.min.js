function setup(type, id, text, href) {
    var element = document.createElement(type);
    element.textContent = text;
    element.setAttribute('href', href);
    element.id = id;
    document.body.appendChild(element);
}
